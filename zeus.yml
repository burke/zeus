---
command: "/usr/bin/env ruby -rzeus -e Zeus::Slave.boot"

plan:
  default_bundle:
    development_environment:
      prerake:
        rake:
      runner:
      console:
    test_environment:
      testtask:
      test_helper:
        testrb:
      spec_helper:
        rspec:

items:
  default_bundle: |
    require 'rails/all'
  development_environment: |
    Bundler.require(:development)
  test_environment: |
    Bundler.require(:test)
  runner: |
    omg


